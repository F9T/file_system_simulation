<UserControl x:Class="FileSystemSimulation.Views.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileSystemSimulation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">
        <DockPanel LastChildFill="True">
            <TextBlock Text="Name :" />
            <TextBox Text="{Binding Name}" Margin="5,0,0,0"/>
        </DockPanel>
        <DockPanel Margin="0,10,0,0" LastChildFill="True">
            <TextBlock Text="Size :" />
            <TextBox Text="{Binding Size}" HorizontalAlignment="Stretch" Margin="5,0,0,0"/>
        </DockPanel>
    </StackPanel>
</UserControl>
